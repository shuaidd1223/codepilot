# Project-level overrides (higher priority than global config)
# Keep only project-specific fields here.

[project]
name = "codepilot-dev"
base_branch = "dev"

# Example: uncomment to override global automation defaults per project
# [automation]
# planner = "claude"
# task_agent = "dual"
