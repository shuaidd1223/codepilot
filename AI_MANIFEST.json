{
  "name": "CodePilot",
  "version": "0.1.0",
  "command_name": "codepilot",
  "description": "本地工程工作流 CLI，可把自然语言需求转换成任务，并自动规划、执行、审查和发布。",
  "calling_principles": [
    "优先使用非交互命令，避免 chat 模式，除非明确需要持续会话。",
    "需要结构化结果时，优先使用 `status --json`、`find --json`、`ai manifest`。",
    "如果目标是提交一个自然语言需求，直接调用 `codepilot \"需求文本\"` 或 `codepilot go \"需求文本\"`。",
    "如果目标是发布产物，优先调用 `codepilot release prepare --version <版本号>`。",
    "如果任务处于运行中，先用 `codepilot status -p <项目名> -v` 查看阶段，再决定是否 `logs` 或 `stop`。",
    "如果任务失败或被取消，且需要重新排队，使用 `codepilot retry <task_id>`。"
  ],
  "structured_outputs": [
    {
      "command": "codepilot ai manifest",
      "format": "json",
      "purpose": "输出完整命令清单、常见工作流和调用建议。"
    },
    {
      "command": "codepilot status -p <项目名> --json",
      "format": "json",
      "purpose": "获取项目任务状态和任务列表。"
    },
    {
      "command": "codepilot find <关键词> -p <项目名> --json",
      "format": "json",
      "purpose": "搜索任务并获取结构化结果。"
    }
  ],
  "commands": [
    {
      "name": "init",
      "syntax": "codepilot init <path>",
      "purpose": "初始化项目并生成 AGENTS.toml。",
      "when_to_use": "第一次接入某个仓库时。",
      "examples": [
        "codepilot init .",
        "codepilot init D:\\repo -n demo"
      ]
    },
    {
      "name": "goal",
      "syntax": "codepilot \"需求文本\"",
      "purpose": "提交一个自然语言需求，让 CodePilot 自动判断是否拆分并按配置执行。",
      "when_to_use": "其他 AI 想把高层目标交给 CodePilot 自己执行时。",
      "examples": [
        "codepilot \"修复任务重试逻辑并补测试\""
      ]
    },
    {
      "name": "status",
      "syntax": "codepilot status -p <项目名> [-v|--json]",
      "purpose": "查看任务总览、运行态、最后输出。",
      "when_to_use": "检查是否还有 backlog、是否有任务卡住、当前运行到哪一阶段。",
      "examples": [
        "codepilot status -p codepilot-dev -v",
        "codepilot status -p codepilot-dev --json"
      ]
    },
    {
      "name": "logs",
      "syntax": "codepilot logs <task_id> [--tail N|--full]",
      "purpose": "查看任务实时日志或历史日志。",
      "when_to_use": "需要了解任务执行细节或错误上下文。",
      "examples": [
        "codepilot logs 7",
        "codepilot logs 7 --tail 50"
      ]
    },
    {
      "name": "stop",
      "syntax": "codepilot stop <task_id> [-m 原因]",
      "purpose": "停止运行中的任务。",
      "when_to_use": "任务卡住、执行方向错误、需要强制终止时。",
      "examples": [
        "codepilot stop 7",
        "codepilot stop 7 -m \"方向错误，停止重跑\""
      ]
    },
    {
      "name": "retry",
      "syntax": "codepilot retry <task_id>",
      "purpose": "手动重试指定任务，重置运行态并重新放回 backlog。",
      "when_to_use": "任务 failed/cancelled 后需要人工重新触发时。",
      "examples": [
        "codepilot retry 7"
      ]
    },
    {
      "name": "run",
      "syntax": "codepilot run -p <项目名> [--executor builtin|dispatch]",
      "purpose": "执行 backlog 中的任务。",
      "when_to_use": "已存在 backlog，想手动触发执行。",
      "examples": [
        "codepilot run -p codepilot-dev --executor builtin --no-auto-commit"
      ]
    },
    {
      "name": "ui",
      "syntax": "codepilot ui [--host 127.0.0.1] [--port 8766]",
      "purpose": "启动本地 Web UI，图形化查看项目、任务和状态，并执行重试 / 停止 / 插队。",
      "when_to_use": "需要图形化总览多个项目，或需要人工点击干预任务时。",
      "examples": [
        "codepilot ui",
        "codepilot ui --no-open --port 8877"
      ]
    },
    {
      "name": "release_prepare",
      "syntax": "codepilot release prepare --version <版本号>",
      "purpose": "更新版本号、构建当前平台、生成发布目录并自动校验。",
      "when_to_use": "准备一个可交付的本地发布包时。",
      "examples": [
        "codepilot release prepare --version 0.1.1"
      ]
    },
    {
      "name": "release_bundle",
      "syntax": "codepilot release bundle [--build-current] [--artifact 平台=路径]",
      "purpose": "把已有二进制整理成标准发布目录。",
      "when_to_use": "已有一个或多个平台二进制，准备打包发布时。",
      "examples": [
        "codepilot release bundle --build-current",
        "codepilot release bundle --artifact linux-x86_64=dist/binary/linux-x86_64/codepilot"
      ]
    },
    {
      "name": "release_verify",
      "syntax": "codepilot release verify [--release-dir 发布目录]",
      "purpose": "校验发布目录、压缩包内容和校验值。",
      "when_to_use": "发布前自检或 CI 验证时。",
      "examples": [
        "codepilot release verify",
        "codepilot release verify --release-dir dist/release/codepilot-0.1.0-summary"
      ]
    }
  ],
  "workflows": [
    {
      "name": "提交一个新需求并执行",
      "steps": [
        "codepilot init .",
        "codepilot \"实现一个需求\"",
        "codepilot status -p <项目名> -v",
        "codepilot logs <task_id>"
      ]
    },
    {
      "name": "排障运行中的任务",
      "steps": [
        "codepilot status -p <项目名> -v",
        "codepilot logs <task_id> --tail 80",
        "codepilot stop <task_id>"
      ]
    },
    {
      "name": "重试失败或取消的任务",
      "steps": [
        "codepilot logs <task_id> --tail 80",
        "codepilot retry <task_id>",
        "codepilot run -p <项目名>"
      ]
    },
    {
      "name": "准备一个发布包",
      "steps": [
        "codepilot release prepare --version <版本号>",
        "codepilot release verify"
      ]
    }
  ]
}