interface:
  display_name: "CodePilot Workflow"
  short_description: "Run CodePilot requirement workflows via CLI."
  default_prompt: "Use $codepilot-workflow to process a requirement, execute tasks, and report status and logs."

policy:
  allow_implicit_invocation: true
