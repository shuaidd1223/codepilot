【判定规则】
- 只要有任意一条验收标准未通过或无法判断, 就必须判定 FAIL。
- 功能正确性、回归风险、遗漏的验证也要一并看，但不要替代验收标准的逐条检查。
- 如果 FAIL, 请先给出 '需要修复的点' 列表（可被 builder 直接拿去改），再给 VERDICT 行。

【输出格式】
- 逐条打勾/叉的结论在前；
- 最后**单独一行**输出：`VERDICT: PASS` 或 `VERDICT: FAIL`。
