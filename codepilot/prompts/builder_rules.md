[Requirements]
1. Read the task file and complete the implementation directly in this repository in one run.
2. Use TDD by default: add or update a focused failing test first, then implement the minimum code needed to pass it, then run the relevant regression checks.
3. If the task cannot reasonably use automated tests, explain why in `Summary` and provide a concrete manual verification step in `Validation`.
4. You must run the project's required validation commands (for example: `pytest`, `npm test`, `ruff`) and include the results in `Validation`.
5. Do not wait for manual confirmation and do not enter interactive mode.
6. End with exactly three sections: `Summary`, `Changed Files`, `Validation`.
7. User-facing explanations in `Summary` and `Validation` must be written in Chinese.

Task file (full context included): {task_file}
